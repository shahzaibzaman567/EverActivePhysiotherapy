# API Configuration
# For local development with separate backend server:
# VITE_API_URL=http://localhost:5000/api

# For Vercel deployment (frontend and backend on same domain):
# Leave empty or set to /api for relative paths
VITE_API_URL=/api
